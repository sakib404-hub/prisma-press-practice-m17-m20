enum ActiveStatus {
  ACTIVE
  INACTIVE
}

enum Role {
  USER
  ADMIN
  AUTHOR
}

enum PostStatus {
  PUBLISHED
  ARCHIVED
}

enum CommentStatus {
  APPROVED
  REJECT
  PENDING
}

enum SubscriptionStatus {
  ACTIVE
  CANCELLED
  EXPIRED
}
